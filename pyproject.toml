[build-system]
requires = ["setuptools>=83", "wheel>=0.41"]
build-backend = "setuptools.build_meta"

[project]
name = "era-screening"
version = "1.0.0rc3"
description = "Auditable layer-wise drift screening for related language-model checkpoints"
readme = "README.md"
license = { text = "Apache-2.0" }
requires-python = ">=3.10"
authors = [
  { name = "Alexander Paolo Zeisberg Militerni", email = "alexander.zeisberg85@gmail.com" },
]
classifiers = [
  "Development Status :: 4 - Beta",
  "Intended Audience :: Science/Research",
  "Topic :: Scientific/Engineering :: Artificial Intelligence",
  "License :: OSI Approved :: Apache Software License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
]

# Keep supported ranges in sync with requirements.txt.
# torch is intentionally NOT listed: install the CPU or CUDA build explicitly,
# see README "Quick Start".
# Core = what `era/` imports (screening an existing pair). The PoC training
# and plotting harness in experiments/ needs the `experiments` extra.
dependencies = [
  "numpy>=1.23,<3.0",
  "packaging>=23.2",
  "transformers>=5.16.1,<5.17",
  "tokenizers>=0.23.2,<0.24",
  "safetensors>=0.8.0",
  "sentencepiece>=0.1.99",
]

[project.optional-dependencies]
experiments = [
  # Tested with Transformers 5.16; keep these minor release lines together.
  "accelerate>=1.14,<1.15",
  "datasets>=5.0.1,<5.1",
  "matplotlib>=3.7,<4.0",
  "pandas>=2.0,<4.0",
  "scipy>=1.11,<2.0",
]
dev = [
  "pytest>=7.3.0",
  "pytest-cov>=4.1.0",
  "black>=23.3.0",
  "flake8>=6.0.0",
  "mypy>=1.3.0",
]

[project.urls]
Homepage = "https://github.com/blacklotus1985/era-screening"
Documentation = "https://github.com/blacklotus1985/era-screening/tree/main/docs"
Issues = "https://github.com/blacklotus1985/era-screening/issues"

[tool.setuptools]
packages = ["era"]
license-files = ["LICENSE", "NOTICE"]
