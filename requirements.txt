# Supported dependency ranges — keep in sync with pyproject.toml.
#
# PyTorch is deliberately NOT pinned here: install the CPU or CUDA build
# explicitly (see README "Quick start").
# The canonical era/ pipeline is NumPy-only. SciPy is required only by two
# experiment-analysis scripts and therefore belongs to the experiments list.

# --- Core: screening an existing (base, finetuned) pair -----------------
numpy>=1.23,<3.0
transformers>=5.16.1,<5.17
tokenizers>=0.23,<0.24
safetensors>=0.4.3
sentencepiece>=0.1.99

# --- Experiments extra: PoC training + plotting harness -----------------
# (pip install -e .[experiments])
# accelerate is a transitive dep of the HF Trainer; pinned explicitly because
# the >=1.0 series dropped the `dispatch_batches` kwarg that this Transformers line
# still passes to Accelerator, which breaks training with a TypeError.
accelerate>=0.27,<0.30
datasets>=2.14,<2.19
matplotlib>=3.7,<4.0
pandas>=2.0,<4.0
scipy>=1.11,<2.0
